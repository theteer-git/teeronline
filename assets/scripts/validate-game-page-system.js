const fs=require("node:fs"),path=require("node:path"),vm=require("node:vm");const root=process.cwd();const files=["assets/templates/game-page.html","assets/css/game-page.css","assets/scripts/game-page.js","assets/scripts/build-game-pages.js","assets/scripts/game-config.js"];for(const file of files){if(!fs.existsSync(path.join(root,file)))throw new Error(`Missing ${file}`);}new vm.Script(fs.readFileSync(path.join(root,"assets/scripts/game-page.js"),"utf8"),{filename:"game-page.js"});require("./build-game-pages.js");const out=path.join(root,"dist-game-preview");const html=fs.readdirSync(out).filter(x=>x.endsWith(".html"));if(html.length!==8)throw new Error(`Expected 8 previews; found ${html.length}`);for(const name of html){const text=fs.readFileSync(path.join(out,name),"utf8");for(const id of ["live_result","previous_7_days","common_numbers","statistics","related_games"]){if(!text.includes(`id="${id}"`))throw new Error(`${name}: missing ${id}`);}if(/\{\{/.test(text))throw new Error(`${name}: unresolved token`);if((text.match(/data-game-id=/g)||[]).length!==1)throw new Error(`${name}: invalid game boundary`);if(text.includes("all-results.json"))throw new Error(`${name}: forbidden all-results endpoint`);}console.log("Shared game-page system validation: PASS");console.log("Preview pages generated: 8");console.log("Semantic sections: PASS");console.log("Single-game DOM boundary: PASS");console.log("all-results.json boundary: PASS");console.log("JavaScript syntax: PASS");
